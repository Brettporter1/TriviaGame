// GLOBAL VARIABLES
let topics = ['forrest Gump',]



// pull default topics within the array
var showDefaultButtons = function(arr){
    for (var i = 0; i < arr.length;i++){
        var buttonTopic = arr[i];
        const btn = $('<div>');

    }
};

// push user input to topic array and display gifs



